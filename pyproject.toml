[tool.poetry]
name = "rest_app"
version = "0.1.0"
description = ""
authors = ["Finn Gruwier Larsen <finn@gruwier.dk>"]

[tool.poetry.dependencies]
python = "^3.8"

[tool.poetry.dev-dependencies]
pytest = "^5.2"
fastapi-code-generator = "*"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
